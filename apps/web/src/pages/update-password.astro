---
import Layout from '../layouts/Layout.astro';
import UpdatePasswordForm from '../components/UpdatePasswordForm';
---

<Layout title="Update Password - UniPick">
  <div class="min-h-[80vh] flex flex-col justify-center items-center px-4">
    <div class="w-full max-w-md bg-white p-8 rounded-2xl shadow-xl border border-gray-100">
      {/* client:only="react" 是必须的！
        因为 Supabase 需要读取 URL 里的 #access_token 才能登录用户。
        如果服务端渲染了这个组件，它会发现没登录，直接报错。
      */}
      <UpdatePasswordForm client:only="react" />
    </div>
  </div>
</Layout>